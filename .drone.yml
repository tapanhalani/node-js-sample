kind: pipeline
type: exec
name: tapan_dockerized_pipeline

steps:
# - name: npm_install
#   image: node:8.16.0-jessie-slim
#   commands:
#   - npm install
- name: docker_build_image
  # image: docker:19.03.1
  commands:
  - docker build -t tapanhalani/sample-nodejs-app .